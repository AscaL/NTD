import React from 'react';

export default class CreateTodos extends React.Component {
  render() {
    return (
      <form>
        <input type="text" />
        <button>Create</button>
      </form>
    )
  }
}